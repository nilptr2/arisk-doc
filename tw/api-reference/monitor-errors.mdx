---
title: "監控服務錯誤碼"
description: "監控地址及告警推送相關的業務錯誤碼及其含義。"
icon: "triangle-exclamation"
---

### 響應結構

當 API 調用發生錯誤時，將返回包含以下字段的 JSON 響應：

- `code`: 6 位數字的業務錯誤碼。
- `message`: 錯誤的具体描述信息。

範例：
```json
{
  "code": "104001",
  "message": "Invalid request parameters (請求參數無效)"
}
```


### 錯誤碼格式

業務錯誤碼的第一位數字代表錯誤的級別或類型：

- **1**: 參數或輸入錯誤。
- **2**: 業務邏輯錯誤。
- **3**: 系統或內部錯誤。

---

<ResponseField name="105002" type="InvalidArgument">
  Chain or token is required (鏈或代幣必填)
</ResponseField>

<ResponseField name="105003" type="InvalidArgument">
  Invalid pagination parameter (分頁參數無效)
</ResponseField>

<ResponseField name="105004" type="InvalidArgument">
  Event type is invalid (事件類型無效)
</ResponseField>

<ResponseField name="105005" type="InvalidArgument">
  Addresses cannot be empty (地址列表不能為空)
</ResponseField>

<ResponseField name="105006" type="InvalidArgument">
  Too many addresses in a single request (單次請求地址過多)
</ResponseField>

<ResponseField name="205001" type="NotFound">
  Monitor address not found (未找到監控地址)
</ResponseField>

<ResponseField name="205002" type="NotFound">
  Alert not found (未找到告警記錄)
</ResponseField>

<ResponseField name="205003" type="AlreadyExists">
  Monitor address already exists (監控地址已存在)
</ResponseField>

<ResponseField name="205010" type="NotFound">
  No matched monitored address (未匹配到監控地址)
</ResponseField>

<ResponseField name="205011" type="FailedPrecondition">
  Credits insufficient for overage slots (額度不足以支持額外的監控位)
</ResponseField>

<ResponseField name="305010" type="Unavailable">
  System busy, please try again later (系統繁忙，請稍後再試)
</ResponseField>
